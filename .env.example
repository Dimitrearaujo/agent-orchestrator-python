# Anthropic — necessário para substituir o mock por chamadas reais à API do Claude
ANTHROPIC_API_KEY=sk-ant-...

# Configurações do orquestrador
HITL_CONFIDENCE_THRESHOLD=0.75
HANDOFF_THRESHOLD=0.50
DEFAULT_AGENT=suporte
